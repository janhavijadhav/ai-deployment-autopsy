# Copy this to .streamlit/secrets.toml (never commit secrets.toml)
# On Streamlit Cloud: add these in Settings → Secrets

# Free Groq API key — get one at console.groq.com (no credit card)
GROQ_API_KEY = "gsk_..."
